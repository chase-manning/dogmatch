import styled from "styled-components";

const StyledFooter = styled.div`
  width: 100%;
  padding: 1rem;
  display: flex;
  align-items: center;
  justify-content: center;
  border: solid 1px yellow;
`;

const Footer = () => {
  return <StyledFooter>meow</StyledFooter>;
};

export default Footer;
